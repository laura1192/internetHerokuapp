class KeyPresses {
  NAMES = {
    keyPressesLink: "Key Presses",
    keyPressesTitle: "Key Presses",
    linkName: "/key_presses",
  };

  LOCATORS = {
    target: "#target",
    inputType:"#input[type=text]",
    result:'#result'
  };
}
export const keyPresses = new KeyPresses();
